<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="LOG_DIR">${sys:catalina.base}/logs/lpu-reservation-system/</Property>
       
 <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss} [%t] %highlight{%-5level} - %msg%n
        </Property>
         <Property name="LOG_PATTERNFILE">
            %d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n
        </Property>
    </Properties>

    <Appenders>
        <Console name="Console">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile
            name="File"
            fileName="${LOG_DIR}/lpu-reservation-system.log"
            filePattern="${LOG_DIR}/lpu-reservation-system-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${LOG_PATTERNFILE}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>

</Configuration>
